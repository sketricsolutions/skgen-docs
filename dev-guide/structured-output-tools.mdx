---
title: "Structured Output for Developers"
description: "Define schema-validated JSON tools, detect their public API stream events, and render reliable application UI from SketricGen agent runs."
keywords: ["structured output", "JSON schema tool", "AI agent UI", "tool call event", "SketricGen API"]
---

Structured Output is a model-callable tool that emits schema-validated JSON as the result of the current agent turn. It is useful when your application needs to render a card, save typed project state, trigger routing, or pass a predictable payload to downstream code while the conversation continues across turns.

<Note>
A successful Structured Output call ends the current turn after its tool result is emitted, so the model does not duplicate the payload in a second prose response. It does not collect a person's answer: use Human Input for that, or send the frontend's next message on the same conversation to continue.
</Note>

## Configure the tool

In the Agent Space editor:

1. Add **Structured Output** from the advanced tools section.
2. Give it a short title that describes the event, such as `Render recommendations`.
3. Describe exactly when the agent should call it and what the receiving application will do with the payload.
4. Add at least one output field and mark every field your application requires.
5. Connect the tool to the agent that should emit it, save, and test the run.

A workflow or template stores the node in this form:

```json
{
  "id": "tool-node-recommendations-7d91c2a4",
  "type": "toolNode",
  "data": {
    "name": "emit_render_recommendations_7d91c2a4",
    "title": "Render recommendations",
    "toolType": "structured_output",
    "description": "Research is complete and the frontend needs recommendation cards.",
    "is_enabled": true,
    "config": {
      "version": 1,
      "output_schema": {
        "$schema": "http://json-schema.org/draft-07/schema#",
        "type": "object",
        "properties": {
          "templates": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "slug": { "type": "string" },
                "reason": { "type": "string" }
              },
              "required": ["slug", "reason"]
            }
          }
        },
        "required": ["templates"]
      }
    }
  }
}
```

The editor generates `data.name` deterministically from the title and node ID. When building template JSON programmatically, use the current SketricGen template builder instead of inventing this value.

## Use a zero-argument UI event

When a frontend only needs a signal to display the canonical onboarding preferences form, use the fixed `user_preferences_form` custom tool instead of inventing a Structured Output schema:

```json
{
  "id": "tool-node-user-preferences-form",
  "type": "toolNode",
  "data": {
    "name": "user_preferences_form",
    "toolType": "custom_tool",
    "description": "Show the role and AI-experience form with no arguments as the final action, end the turn, and expect the selections in the user's next message.",
    "is_enabled": true,
    "config": {}
  }
}
```

The call emits ordinary `TOOL_CALL_START` and `TOOL_CALL_END` events with an empty arguments object, then ends the current turn. It does not create a HITL pause. Render the form when the event arrives, then send the selected role and AI experience as the next normal message using the same conversation ID. Do not send a HITL-resume request.

## Schema rules

- The root must be a non-empty object schema.
- Field names must start with a letter and contain only letters, numbers, and underscores.
- Supported field types are string, integer, number, boolean, object, and array.
- Arrays can contain supported primitives or objects.
- Every name in `required` must exist in `properties` at that level.
- Nesting cannot exceed eight levels.
- The current contract version is `1`.

Keep the payload focused. A small schema is easier for lower-cost models to satisfy and easier for your frontend to evolve safely.

## Detect the event

The public streaming API exposes ordinary tool-call events. Match the generated tool name or maintain a node-name mapping in your application.

```ts
function handleAgentEvent(event: Record<string, unknown>) {
  if (
    event.type === "TOOL_CALL_START" &&
    typeof event.tool_call_name === "string" &&
    event.tool_call_name.startsWith("emit_render_recommendations_")
  ) {
    const payload = event.tool_call_arguments as {
      templates: Array<{ slug: string; reason: string }>;
    };

    renderTemplateCards(payload.templates);
  }
}
```

`TOOL_CALL_START.tool_call_arguments` contains the model-supplied object. `TOOL_CALL_END.tool_call_result` contains the validated compact JSON result. Treat the end event as confirmation that runtime validation succeeded and the current agent turn is complete.

## Instruction pattern

Tell the agent what must happen before the call and what it must do afterward:

```text
After you have the user's confirmed goal and a successful template-catalog response,
call Render recommendations with two or three catalog slugs. Do not invent slugs.
Call it as the final action of the onboarding turn, then stop.
```

Do not ask the model to place raw JSON in prose. Let the tool schema validate the fields and let the frontend own rendering, persistence, and navigation.

## Security and reliability

- Validate identifiers such as template slugs against server-trusted data before navigation or writes.
- Do not put API keys or private credentials in the output schema, description, or tool arguments.
- Make write actions idempotent and require confirmation separately; Structured Output itself only returns data.
- Version your frontend component contract inside the payload when UI compatibility matters.
- Test missing context, invalid values, duplicate tool calls, interrupted streams, and model retries.

The former pre-release shape using `toolType: "custom_tool"` with `config.kind: "structured_output"` is not supported. Use `toolType: "structured_output"` and omit `config.kind`.
